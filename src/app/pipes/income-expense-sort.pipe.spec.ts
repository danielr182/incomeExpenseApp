import { IncomeExpenseSortPipe } from './income-expense-sort.pipe';

describe('IncomeExpenseSortPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeExpenseSortPipe();
    expect(pipe).toBeTruthy();
  });
});
